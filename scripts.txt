CREATE TABLE USER ( ID BIGSERIAL PRIMARY KEY, FIRST_NAME VARCHAR(250) NOT NULL, LAST_NAME VARCHAR(250) NOT NULL, EMAIL VARCHAR(250) NOT NULL, DATE_CREATED TIMESTAMP WITH TIME ZONE NOT NULL, DATE_UPDATED TIMESTAMP WITH TIME ZONE NOT NULL)